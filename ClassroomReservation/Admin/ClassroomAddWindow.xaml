<Window x:Class="ClassroomReservation.Main.ClassroomAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClassroomReservation.Main"
        mc:Ignorable="d"
        Title="강의실 관리" SizeToContent="Height" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Border Padding="20">
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox Name="inputBox" Grid.Column="0" Margin="0 0 8 0"/>
                <Button Name="addButton" Grid.Column="1" Content="추가"/>
            </Grid>
            <ListBox Name="listBox" Margin="0 10 0 0"/>
            <Button Name="deleteButton" Content="삭제" HorizontalAlignment="Right" Margin="0 10 0 0"></Button>
        </StackPanel>
    </Border>
</Window>
